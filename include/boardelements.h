#pragma once

namespace elements
{

enum class Figures : char { WHITE = 'W', YELLOW = 'Y', ORANGE = 'O', GREEN = 'G', BLUE = 'B' };
enum class Bonuses : char { PLUS = '+', MINUS = '-' };

} // namespace elements
