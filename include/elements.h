#pragma once

namespace elements
{

enum class Figures : char { WHITE = 'W', YELLOW = 'Y', ORANGE = 'O', GREEN = 'G', BLUE = 'B' };
enum class BonusTiles : char { PLUS = '+', MINUS = '-' };
enum class StateChars : char { SEPARATOR = ';', WINSTART = '>', WINEND = '<' };

} // namespace elements
